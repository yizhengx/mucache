kind: Service
apiVersion: v1
metadata:
  name: "service10"
  labels:
    app: "service10"
spec:
  selector:
    app: "service10"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      name: http
    - protocol: TCP
      port: 5550
      targetPort: 5550
      name: zmq1
    - protocol: TCP
      port: 5551
      targetPort: 5551
      name: zmq2
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "service10"
  labels:
    app: "service10"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "service10"
  template:
    metadata:
      labels:
        app: "service10"
    spec:
      containers:
        - name: "service10"
          image: sleepymug/slowpoke-longchain-microbench
          env:
            - name: APP_PORT
              value: "3000"
            - name: APP_NAME
              value: "servicex"
            - name: APP_NAME_NO_UNDERSCORES
              value: "service10"
            - name: APP_NAMESPACE
              value: "longchain"
            - name: CM_ENABLED
              value: "false"
            - name: EXPIRATION_TTL
              value: "0"
            - name: SLOWPOKE_DELAY_MICROS
              value: "${SLOWPOKE_DELAY_MICROS_SERVICE10}"
            - name: SLOWPOKE_PRERUN
              value: "${SLOWPOKE_PRERUN}"
            - name: SLOWPOKE_PROCESSING_MICROS
              value: "${SLOWPOKE_PROCESSING_MICROS_SERVICE10}"
            - name: LONGCHAIN_SERVICE_NUM
              value: "10"
          ports:
            - containerPort: 3000
            - containerPort: 5550
            - containerPort: 5551
          imagePullPolicy: Always

      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - worker11
