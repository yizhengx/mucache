kind: Service
apiVersion: v1
metadata:
  name: "currency"
  labels:
    app: "currency"
spec:
  selector:
    app: "currency"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      name: http
    - protocol: TCP
      port: 5550
      targetPort: 5550
      name: zmq1
    - protocol: TCP
      port: 5551
      targetPort: 5551
      name: zmq2
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "currency"
  labels:
    app: "currency"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "currency"
  template:
    metadata:
      labels:
        app: "currency"
    spec:
      containers:
        - name: "currency"
          image: yizhengx/mucache:boutique-poker-time-spinning
          env:
            - name: APP_PORT
              value: "3000"
            - name: APP_NAME
              value: "currency"
            - name: APP_NAME_NO_UNDERSCORES
              value: "currency"
            - name: APP_NAMESPACE
              value: "boutique"
            - name: NODE_IDX
              value: "3"
            - name: CM_ENABLED
              value: "false"
            - name: EXPIRATION_TTL
              value: "0"
            - name: SLOWPOKE_PROCESSING_MICROS
              value: "${SLOWPOKE_PROCESSING_MICROS_CURRENCY}"
            - name: SLOWPOKE_DELAY_MICROS
              value: "${SLOWPOKE_DELAY_MICROS_CURRENCY}"
            - name: SLOWPOKE_PRERUN
              value: "${SLOWPOKE_PRERUN}"
            - name: SLOWPOKE_POKER_BATCH_THRESHOLD
              value: "${SLOWPOKE_POKER_BATCH_THRESHOLD}"
            - name: SLOWPOKE_PROCESSING_MICROS
              value: "${SLOWPOKE_PROCESSING_MICROS_CURRENCY}"
          ports:
            - containerPort: 3000
            - containerPort: 5550
            - containerPort: 5551
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 1000m
              memory: 4Gi
            limits:
              cpu: 1000m
              memory: 4Gi
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - worker3
