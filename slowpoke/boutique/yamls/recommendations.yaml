kind: Service
apiVersion: v1
metadata:
  name: "recommendations"
  labels:
    app: "recommendations"
spec:
  selector:
    app: "recommendations"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      name: http
    - protocol: TCP
      port: 5550
      targetPort: 5550
      name: zmq1
    - protocol: TCP
      port: 5551
      targetPort: 5551
      name: zmq2
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "recommendations"
  labels:
    app: "recommendations"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "recommendations"
  template:
    metadata:
      labels:
        app: "recommendations"
    spec:
      containers:
        - name: "recommendations"
          image: yizhengx/mucache:boutique-poker-time-spinning
          env:
            - name: APP_PORT
              value: "3000"
            - name: APP_NAME
              value: "recommendations"
            - name: APP_NAME_NO_UNDERSCORES
              value: "recommendations"
            - name: APP_NAMESPACE
              value: "boutique"
            - name: NODE_IDX
              value: "2"
            - name: CM_ENABLED
              value: "false"
            - name: EXPIRATION_TTL
              value: "0"
            - name: SLOWPOKE_PROCESSING_MICROS
              value: "${SLOWPOKE_PROCESSING_MICROS_RECOMMENDATIONS}"
            - name: SLOWPOKE_DELAY_MICROS
              value: "${SLOWPOKE_DELAY_MICROS_RECOMMENDATIONS}"
            - name: SLOWPOKE_PRERUN
              value: "${SLOWPOKE_PRERUN}"
            - name: SLOWPOKE_POKER_BATCH_THRESHOLD
              value: "${SLOWPOKE_POKER_BATCH_THRESHOLD}"
            - name: SLOWPOKE_PROCESSING_MICROS
              value: "${SLOWPOKE_PROCESSING_MICROS_RECOMMENDATIONS}"
          ports:
            - containerPort: 3000
            - containerPort: 5550
            - containerPort: 5551
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 1000m
              memory: 4Gi
            limits:
              cpu: 1000m
              memory: 4Gi
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - worker8
